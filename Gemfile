# frozen_string_literal: true

source "https://rubygems.org"

gemspec

# Use the latest version of Jekyll
gem 'jekyll'

# Plugins of this site loaded during a build irrespective of site config

group :jekyll_plugins do
    gem 'jekyll-spaceship'
end
